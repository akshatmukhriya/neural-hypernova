apiVersion: ray.io/v1
kind: RayCluster
metadata:
  name: hypernova-ray
spec:
  rayVersion: '2.9.0'
  headGroupSpec:
    rayStartParams: 
      dashboard-host: "0.0.0.0"
      include-dashboard: "true"
    template:
      spec:
        containers:
          - name: ray-head
            image: rayproject/ray-ml:2.9.0-py310-gpu
            ports:
              - containerPort: 8265
              - containerPort: 6379
              - containerPort: 10001
            resources:
              # FIXED: Reduced to fit on t3.medium (4GB RAM)
              limits:
                cpu: "1"
                memory: "2Gi"
              requests:
                cpu: "500m"
                memory: "1Gi"
  workerGroupSpecs:
    - replicas: 2
      groupName: gpu-group
      rayStartParams: {}
      template:
        spec:
          # Karpenter will see this and spawn G5/P4 instances
          runtimeClassName: nvidia 
          containers:
            - name: ray-worker
              image: rayproject/ray-ml:2.9.0-py310-gpu
              resources:
                limits:
                  nvidia.com/gpu: 1
                  cpu: "4"
                  memory: "16Gi"